export interface IPagination {
    pageNumber?: number,
    pageSize?: number,
    offset?: number,
    total?: number,
    pages?: number,
    current?: number,
    previous?: number,
    next?: number
}
